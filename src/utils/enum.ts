export enum ELanguage {
  TH = "th",
  EN = "en",
  // JP = "jp",
}

export enum EFetchApiMethod {
  GET = "GET",
  POST = "POST",
}

export enum EMatchType {
  BOT = "BOT",
  PLAYER = "PLAYER",
}

export enum EQueueStatus {
  W = "waiting",
  P = "playing",
  C = "complete",
  L = "leave",
}
