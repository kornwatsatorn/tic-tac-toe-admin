import { IPaginate } from "@/types/paginate"

export const defaultImage = "/images/default_image.png"

export const defaultPaginate: IPaginate = {
  page: 1,
  perPage: 20,
  total: 0,
  totalPage: 1,
}
